using Mcm.Shared.Domain.Interfaces;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata;
using System.Reflection;

namespace Mcm.Shared.Infrastructure.Extensions
{
    public static class ModelBuilderExtensions
    {
//         public static void ApplyGlobalFilters(this ModelBuilder modelBuilder, DbContext context)
//         {
//             foreach (var entityType               var clrType = entityType.ClrType;

//                 if (typeof(ISoftDeletable).IsAssignableFrom(clrType))
//                 {   
//                     // var method = typeof(ModelBuilderExtensions)
//                     /

//                     // method/     .GetMethod(nameof(SetSoftDeleteFilter), BindingFlags.NonPublic | BindingFlags.Static)
//                     //     ?.MakeGeneric in modelBuilder.Model.GetEntityTypes())
//             {
//  Method(clrType);?.Invoke(null, new object[] { modelBuilder });
//                 }

//                 // if (typeof(ITenantScoped).IsAssignableFrom(clrType))
//                 // {
//                 //     var method = typeof(ModelBuilderExtensions)
//                 //         .GetMethod(nameof(SetTenantFilter), BindingFlags.NonPublic | BindingFlags.Static)
//                 //         ?.MakeGenericMethod(clrType);

//                 //     method?.Invoke(null, new object[] { modelBuilder, context });
//                 // }
//             }

        
    }
}